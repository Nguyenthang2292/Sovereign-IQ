"""
API Configuration Template for external services.
Copy file này thành config/config_api.py và điền API keys của bạn.

HOẶC tốt hơn: Sử dụng biến môi trường (khuyến nghị cho bảo mật)

File này là template - KHÔNG chứa API keys thực tế.
"""

import os

# Binance API Configuration
# Cách 1 (Khuyến nghị): Sử dụng biến môi trường
#   export BINANCE_API_KEY='your-key-here'
#   export BINANCE_API_SECRET='your-secret-here'
# Cách 2: Điền trực tiếp vào file này (KHÔNG khuyến nghị)
BINANCE_API_KEY = os.getenv("BINANCE_API_KEY") or None  # Thay None bằng 'your-key-here' nếu không dùng biến môi trường
BINANCE_API_SECRET = os.getenv("BINANCE_API_SECRET") or None  # Thay None bằng 'your-secret-here' nếu không dùng biến môi trường

# Google Gemini API Configuration
# Lấy API key từ: https://makersuite.google.com/app/apikey
# Hoặc: https://aistudio.google.com/app/apikey
# Cách 1 (Khuyến nghị): Sử dụng biến môi trường
#   export GEMINI_API_KEY='your-api-key-here'
# Cách 2: Điền trực tiếp vào file này (KHÔNG khuyến nghị)
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY") or None  # Thay None bằng 'your-api-key-here' nếu không dùng biến môi trường

