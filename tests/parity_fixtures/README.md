# Adaptive Trend Parity Fixtures

Generate fixtures and expected outputs from `modules/adaptive_trend` with one command:

```bash
python scripts/generate_adaptive_trend_parity_fixtures.py
```

This command regenerates deterministic scenarios:
- `trend_up`
- `trend_down`
- `sideways`
- `noisy`
- `nan_gaps`
