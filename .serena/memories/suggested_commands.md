Windows project commands: `uv sync`, `uv pip install -e .`; run entrypoints with `python main.py`, `python main_complex_hybrid.py`, `python main_complex_voting.py`, `python main_complex_atc_oscillator.py`, `python run_auto_trade_gui.py`; tests via `run_tests.bat`, `run_tests.ps1`, or `.venv\\Scripts\\python -m pytest`; lint/format/type-check with `ruff check .`, `ruff format .`, `mypy .`; Rust build with `build_rust.bat` or `build_rust.ps1`.